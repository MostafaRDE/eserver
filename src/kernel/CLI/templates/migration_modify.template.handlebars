import Migration from '../../kernel/CLI/Migration/Migration'
import Schema from '../../kernel/CLI/Schema/Schema'

module.exports = class {{ class_name }} extends Migration
{
    async up()
    {
        await Schema.table('{{ table_name }}', (table) =>
        {
            //
        })
    }

    async down()
    {
        //
    }
}